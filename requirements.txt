google-genai>=0.8.0
chromadb>=0.5.0
playwright>=1.40.0
python-dotenv>=1.0.0
flask>=3.0.0
anthropic>=0.40.0        # Claude Sonnet — Game Logics agent (optional, falls back to Gemini)
sentence-transformers    # Better ChromaDB embeddings (optional, has silent fallback)
